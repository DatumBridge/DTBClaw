[package]
name = "octoclaw-core"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Core contracts and boundaries for staged multi-crate extraction."

[lib]
path = "src/lib.rs"

[dependencies]
octoclaw-types = { path = "../octoclaw-types" }
